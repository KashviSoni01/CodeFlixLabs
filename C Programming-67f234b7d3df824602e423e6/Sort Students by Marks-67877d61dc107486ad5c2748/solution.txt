[{"code":"#include <stdio.h>\r\n\r\nint main() {\r\n    struct student {\r\n        int roll;\r\n        char name[100];\r\n        float marks;\r\n    };\r\n\r\n    int n;\r\n    scanf(\"%d\", &n);\r\n    struct student s[n];\r\n\r\n    // Input student data\r\n    for (int i = 0; i < n; i++) {\r\n        scanf(\"%d %s %f\", &s[i].roll, s[i].name, &s[i].marks);\r\n    }\r\n\r\n    // Sorting by marks in descending order using Bubble Sort\r\n    for (int i = 0; i < n - 1; i++) {\r\n        for (int j = 0; j < n - i - 1; j++) {\r\n            if (s[j].marks < s[j + 1].marks) {\r\n                // Swap\r\n                struct student temp = s[j];\r\n                s[j] = s[j + 1];\r\n                s[j + 1] = temp;\r\n            }\r\n        }\r\n    }\r\n\r\n    // Output sorted list\r\n    printf(\"\\nStudents sorted by marks (High to Low):\\n\");\r\n    for (int i = 0; i < n; i++) {\r\n        printf(\"Roll: %d, Name: %s, Marks: %.2f\\n\", s[i].roll, s[i].name, s[i].marks);\r\n    }\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-05-28T00:38:41.115Z","isCorrect":false},{"code":"#include <stdio.h>\r\n\r\nint main() {\r\n    struct student {\r\n        int roll;\r\n        char name[100];\r\n        float marks;\r\n    };\r\n\r\n    int n;\r\n    scanf(\"%d\", &n);\r\n    struct student s[n];\r\n\r\n    // Input student data\r\n    for (int i = 0; i < n; i++) {\r\n        scanf(\"%d %s %f\", &s[i].roll, s[i].name, &s[i].marks);\r\n    }\r\n\r\n    // Sorting by marks in descending order using Bubble Sort\r\n    for (int i = 0; i < n - 1; i++) {\r\n        for (int j = 0; j < n - i - 1; j++) {\r\n            if (s[j].marks < s[j + 1].marks) {\r\n                // Swap\r\n                struct student temp = s[j];\r\n                s[j] = s[j + 1];\r\n                s[j + 1] = temp;\r\n            }\r\n        }\r\n    }\r\n    for (int i = 0; i < n; i++) {\r\n        printf(\"Roll: %d, Name: %s, Marks: %.2f\\n\", s[i].roll, s[i].name, s[i].marks);\r\n    }\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-05-28T00:39:01.343Z","isCorrect":false}]