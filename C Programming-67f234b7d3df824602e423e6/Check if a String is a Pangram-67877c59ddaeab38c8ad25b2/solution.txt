[{"code":"#include <stdio.h>\r\n#include <ctype.h>\r\n#include <string.h>\r\n\r\nint isPangram(char str[]) {\r\n    int alphabet[26] = {0};  // Track each letter a–z\r\n\r\n    for (int i = 0; str[i] != '\\0'; i++) {\r\n        char ch = tolower(str[i]);  // Convert to lowercase\r\n        if (ch >= 'a' && ch <= 'z') {\r\n            alphabet[ch - 'a'] = 1;  // Mark the letter as seen\r\n        }\r\n    }\r\n\r\n    // Check if all letters are seen\r\n    for (int i = 0; i < 26; i++) {\r\n        if (alphabet[i] == 0)\r\n            return 0;  // Not a pangram\r\n    }\r\n    return 1;  // Pangram\r\n}\r\n\r\nint main() {\r\n    char str[1000];\r\n    printf(\"Enter a string: \");\r\n    scanf(\"%[^\\n]\", str);\r\n\r\n    if (isPangram(str)) {\r\n        printf(\"Yes\");\r\n    } else {\r\n        printf(\"No\");\r\n    }\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-05-09T10:56:08.859Z","isCorrect":false},{"code":"#include <stdio.h>\r\n#include <ctype.h>\r\n#include <string.h>\r\n\r\nint isPangram(char str[]) {\r\n    int alphabet[26] = {0};  // Track each letter a–z\r\n\r\n    for (int i = 0; str[i] != '\\0'; i++) {\r\n        char ch = tolower(str[i]);  // Convert to lowercase\r\n        if (ch >= 'a' && ch <= 'z') {\r\n            alphabet[ch - 'a'] = 1;  // Mark the letter as seen\r\n        }\r\n    }\r\n\r\n    // Check if all letters are seen\r\n    for (int i = 0; i < 26; i++) {\r\n        if (alphabet[i] == 0)\r\n            return 0;  // Not a pangram\r\n    }\r\n    return 1;  // Pangram\r\n}\r\n\r\nint main() {\r\n    char str[1000];\r\n    scanf(\"%[^\\n]\", str);\r\n\r\n    if (isPangram(str)) {\r\n        printf(\"Yes\");\r\n    } else {\r\n        printf(\"No\");\r\n    }\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-05-09T10:56:32.585Z","isCorrect":true}]